# Production Environment - Vercel Deployment
# 生产环境 - Vercel 部署

# API 基础 URL - 使用相对路径或生产 API 域名
# 如果 API 和前端在同一域名下，可以使用相对路径 /api
# 如果 API 在不同域名，使用完整 URL
VITE_API_BASE_URL=/api

# 如果需要使用完整的后端 URL，替换为：
# VITE_API_BASE_URL=https://your-api-domain.com
